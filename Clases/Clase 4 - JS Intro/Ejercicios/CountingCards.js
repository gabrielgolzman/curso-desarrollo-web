// Vamos a escribir una función cuenta cartas. Va a recibir una carta como parámetro,
// que puede ser un número o una cadena, y va a incrementar o disminuir el contador global,
// De acuerdo a la tabla que adjuntamos en classroom.

// La función va a retornar una cadena con la cuenta actual y la palabra "Apuesta" si es positiva,
// o "Mantiene" si la cuenta es cero o negativa.
// La cuenta actual y la decisión del jugador ("Apuesta" o "Mantiene") debería estar separada por un solo espacio.

// Salida de ejemplo
// -3 Mantiene
// 5 Apuesta.

//Ayuda
// NO resetees la cuenta a cero cuando el valor es 7, 8 o 9.
// NO devuelvas un arreglo.
// NO incluyas comillas (simples o dobles) en la salida.

var count = 0;

function cc(card) {
	// Cambia el codigo debajo de esta linea

	return 'Cambiame';
}
// Cambia el codigo arriba de esta linea

cc(2);
cc(3);
cc(7);
cc('K');
cc('A');
